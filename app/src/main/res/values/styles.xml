<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colortext</item>
        <item name="android:titleTextStyle">@color/colortext</item>
        <item name="buttonStyle">@style/AppTheme.button</item>
        <item name="titleTextColor">@color/displaylisttextcolor</item>
        <item name="subtitleTextColor">@android:color/white</item>
    </style>

    <!--
    <style name="bekijken" parent="Theme.AppCompat.Light.DarkActionBar">
        <!- Customize your theme here. -->

    <!--
         <item name="colorPrimary">@color/colorPrimary</item>
         <item name="colorPrimaryDark">@color/jemoeder</item>
         <item name="colorAccent">@color/colorAccent</item>
     </style>


     <style name="Theme.bekijken.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar" /> -->
    <!-- Waarom is het stuk hierboven nodig? -->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />  -->



    <style name="AppTheme.button" parent="Widget.AppCompat.Button.Borderless" >
        <item name="android:maxHeight">50dp</item>
        <item name="android:maxWidth">200dp</item>
        </style>

    <style name="AppTheme.button.2" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/displaylisttextcolor</item>
    </style>

</resources>
